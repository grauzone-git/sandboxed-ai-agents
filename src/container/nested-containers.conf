# SSH sessions have no systemd user manager or delegated child cgroups.
[containers]
cgroups = "disabled"
netns = "slirp4netns"

[engine]
cgroup_manager = "cgroupfs"
events_logger = "file"
runtime = "crun"
